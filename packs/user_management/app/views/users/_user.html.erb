<tr>
  <td> <%= user.username %></td>
  <td> <%= user.email %></td>
  <td> <%= user.sign_in_count %></td>
  <td> <%= user.last_sign_in_at %></td>
  <td> <%= user.roles.map(&:name).join(',') %></td>
  <td> 
  <%= link_to "Edit", edit_user_path(user) %> 
  <%= link_to "Create hotel", hotels_path %> 
  <%= button_to "Destroy", user_path(user), method: :delete %>
  </td>
</tr>