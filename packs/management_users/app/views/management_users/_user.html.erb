<tr>
  <td> <%= user.username %></td>
  <td> <%= user.email %></td>
  <td> <%= user.sign_in_count %></td>
  <td> <%= user.last_sign_in_at %></td>
  <td> <%= user.roles.map(&:name).join(',') %></td>
  <td> 
  <%= link_to "Edit", edit_management_user_path(user) %> 
  <%= button_to "Destroy", management_user_path(user), method: :delete %>
  </td>
</tr>