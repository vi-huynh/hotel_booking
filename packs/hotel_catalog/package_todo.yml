# This file contains a list of dependencies that are not part of the long term plan for the
# 'packs/hotel_catalog' package.
# We should generally work to reduce this list over time.
#
# You can regenerate this file using the following command:
#
# bin/packwerk update-todo
---
".":
  "::ApplicationController":
    violations:
    - dependency
    - privacy
    files:
    - packs/hotel_catalog/app/controllers/hotel_catalogs_controller.rb
  "::ApplicationService":
    violations:
    - dependency
    files:
    - packs/hotel_catalog/app/services/hotel_catalogs/search.rb
packs/hotel_management:
  "::Hotel":
    violations:
    - dependency
    files:
    - packs/hotel_catalog/app/controllers/hotel_catalogs_controller.rb
    - packs/hotel_catalog/app/models/search_room_form.rb
